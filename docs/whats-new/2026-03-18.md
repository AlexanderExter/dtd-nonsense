# Maintenance Briefing — 2026-03-18

## Executive Summary

17 packages upgraded across 3 tiers (1 toolchain, 7 framework, 9 utility), including 4 major version bumps (Astro 5→6, Zod 3→4, jsdom 28→29, @vercel/analytics 1→2). All 107 Biome React warnings resolved and rules promoted to `error`. VS Code workspace settings fixed for agent terminal usage. Maintenance prompt updated with lessons learned. 3 config files created/modified, CI pipeline brought to full parity with local checks, 2 pre-existing test failures resolved. Dependency tree reduced from 977 to 883 packages. Zero regressions — all checks, tests, and builds pass.

## Config Changes

| Config File | Change | Rationale |
|-------------|--------|-----------|
| `.editorconfig` | Created | Matches Biome formatter settings; ensures consistent formatting in non-linted files (TOML, YAML) and GitHub web editor |
| `.gitattributes` | Added `*.webp binary`, `bun.lock merge=binary`, linguist hints | WebP binary handling, prevent lockfile merge conflicts, improve GitHub language stats |
| `.github/workflows/build.yml` | Node 20→22, added sync-check/check:deps/check:structure | Pipeline parity with local `bun run check`; Node 22 matches Astro 6 engine requirement |
| `biome.json` | Schema 2.4.6→2.4.7, 3 React rules promoted to `error` | 3 newly-promoted React rules: all 107 violations resolved, rules set to `error` |
| `.vscode/settings.json` | Terminal profile fixed, stale settings removed | Both `terminal.integrated.defaultProfile.windows` and `chat.tools.terminal.terminalProfile.windows` set to Git Bash |
| `.vscode/extensions.json` | Added `unwantedRecommendations` | `esbenp.prettier-vscode` listed as unwanted (conflicts with Biome) |
| `.github/prompts/project-maintenance.prompt.md` | Updated with session lessons | Terminal guidance, no-deferral policy, Biome JSX suppression technique, subagent corruption warning |
| `scripts/__tests__/check-structure.test.ts` | Shared ts-morph Project instance | Fixed 2 pre-existing test timeouts (5000ms→953ms) by eliminating redundant project parsing |
| `package.json` | Added `engines.node`, updated override comments | Astro 6 engine requirement; clarified override rationale |

## Upgrade Manifest

| Package | From | To | Bump | Tier | Notes |
|---------|------|----|------|------|-------|
| `@biomejs/biome` | 2.4.6 | 2.4.7 | patch | Toolchain | 3 new React rules promoted to recommended |
| `astro` | 5.18.0 | 6.0.5 | **major** | Framework | MDX parser stricter on HTML syntax |
| `@astrojs/react` | 4.4.2 | 5.0.0 | **major** | Framework | Peer dep on astro@^6 |
| `@astrojs/starlight` | 0.37.7 | 0.38.1 | minor | Framework | Peer dep on astro@^6 |
| `@astrojs/starlight-tailwind` | 4.0.2 | 5.0.0 | **major** | Framework | Peer dep on @astrojs/starlight@>=0.38 |
| `@astrojs/vercel` | 9.0.4 | 10.0.1 | **major** | Framework | Peer dep on astro@^6 |
| `@astrojs/check` | 0.9.7 | 0.9.8 | patch | Framework | — |
| `@vercel/analytics` | 1.6.1 | 2.0.1 | **major** | Framework | API unchanged for Astro usage |
| `react` | 19.1.0 | 19.2.4 | minor | Utility | — |
| `react-dom` | 19.1.0 | 19.2.4 | minor | Utility | — |
| `@types/react` | 19.1.8 | 19.2.14 | minor | Utility | — |
| `@types/react-dom` | 19.1.6 | 19.2.3 | minor | Utility | — |
| `zod` | 3.25.76 | 4.3.6 | **major** | Utility | All 12 schemas pass without changes |
| `jsdom` | 28.1.0 | 29.0.0 | **major** | Utility | All tests pass without changes |
| `knip` | 5.86.0 | 5.88.0 | minor | Utility | — |
| `rumdl` | 0.1.45 | 0.1.53 | patch | Utility | — |
| `zustand` | 5.0.5 | 5.0.12 | patch | Utility | — |

## Actionable Intelligence

- **Biome 2.4.7** promoted `noArrayIndexKey`, `useExhaustiveDependencies`, and `useHookAtTopLevel` to recommended severity. All 107 violations resolved: 41 auto-fixed, 35 restructured manually (hooks moved above early returns, functions converted to `useCallback`), 31 suppressed with `biome-ignore` (editable form lists with no stable key). Rules promoted from `warn` to `error`.
- **Biome `react` domain** is available and can now be enabled — all violations are resolved. Adding `"react": "recommended"` to domains would provide ongoing React-specific linting.
- **Astro 6** removes the `output` option (static is default). The current `output: "static"` in config is now a no-op but harmless. Can be removed for clarity.
- **Zod 4** shipped with zero breaking changes for this project's schema patterns. All 12 data files validate identically.
- **React 19.2** includes compiler optimizations and improved hydration. No code changes needed.

## Dead Code Removed

Knip found zero issues — no dead files, exports, types, or dependencies.

## Resolution Log

| Breaking Change | Resolution |
|----------------|------------|
| Astro 6: MDX parser rejects HTML comments (`<!-- -->`) | Converted 29 comments to JSX syntax (`{/* */}`) across 15 book files |
| Astro 6: MDX parser rejects bare `<` before digits | Escaped 13 instances (`<1`, `<3`, `<5%`) to `&lt;` entities across 5 content files |
| Biome 2.4.7: 3 React rules promoted to recommended | All 107 violations resolved: auto-fix for `useExhaustiveDependencies` (41), manual restructuring for `useHookAtTopLevel` (31 — hooks moved above early returns), stable keys or `biome-ignore` for `noArrayIndexKey` (35). Rules promoted to `error` in biome.json |
| Biome JSX suppression placement | `biome-ignore` comments must be placed INSIDE the JSX tag as attribute-level comments directly before the `key` prop — NOT before the opening tag |
| Pre-existing: 2 test timeouts in check-structure.test.ts | Shared single ts-morph Project instance; tests now complete in ~950ms vs timing out at 5000ms |

## Security

- **0 known vulnerabilities** in the dependency tree (npm audit clean)
- **2 overrides retained:**
  - `path-to-regexp: ^8.0.0` — ReDoS fix for `@vercel/routing-utils@5.3.3` (still ships `path-to-regexp@6.1.0`)
  - `svgo: ^4.0.1` — DoS fix (GHSA-xpqw-6gx7-v673); Astro 6 declares `svgo ^4.0.0` which could resolve to vulnerable 4.0.0

## CI Changes

| Change | Detail |
|--------|--------|
| Node version | 20 → 22 (matches `engines.node` and Astro 6 requirement) |
| Pipeline parity | Added `sync-check`, `check:deps`, `check:structure` steps — CI now mirrors full local `bun run check` |
| Step order | Reordered: tests before lint (mirrors local check sequence) |

## Lockfile Decision

**Kept committed.** `bun.lock` is committed and tracked. For this single-developer application with agent-driven development, reproducible builds outweigh the merge friction cost.

## Pinning Decisions

- `@biomejs/biome` remains exact-pinned (`2.4.7`) — linter behavior must be deterministic
- All other packages use caret ranges — appropriate for libraries

## Tree Health

- **883 packages** (down from 977 — 94 removed by major upgrades)
- **0 unmet peer dependencies**
- **Engine requirement:** `node ^20.19.1 || >=22.12.0` (added to package.json, matches CI and Astro 6)

## Recommendations

1. ~~**Fix Biome React warnings (107)**~~ — **DONE.** All 107 violations resolved, rules promoted to `error`.
2. ~~**Remove `output: "static"` from astro.config.mjs**~~ — **DONE.** Removed; static is the Astro 6 default.
3. ~~**Enable Biome `react` domain**~~ — **DONE.** Added `"react": "recommended"` to `linter.domains`. Zero new violations.
4. **Monitor `@vercel/routing-utils`** — Still on v5.3.3 with `path-to-regexp-updated@6.3.0`. Override still required. Check next maintenance cycle.
5. ~~**Simplify `engines.node` to `>=22.12.0`**~~ — **DONE.** Node 20 EOL April 2026; CI already uses Node 22.
